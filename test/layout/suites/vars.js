const domain = 'http://localhost:3000'; // eslint-disable-line no-unused-vars
const specPath = './test/layout/specs'; // eslint-disable-line no-unused-vars

const devices = { // eslint-disable-line no-unused-vars
  desktopChrome: {
    deviceName: 'desktop chrome',
    size: '1300x800',
    tags: ['desktop'],
    browser: 'chrome'
  },
  mobileChrome: {
    deviceName: 'mobile chrome',
    size: '459x800',
    tags: ['mobile'],
    browser: 'chrome'
  }
};
